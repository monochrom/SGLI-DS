# Tag

Figma: `tag` (1969:6853) auf 🗄️ Modules. Kein Component-Set, keine States.

```html
<span class="tag">Heute</span>
```

Padding `space-6` / `space-8`, Label-S, Fläche `color/accent/highlight`, Text `color/text/primary` wie in Figma.
